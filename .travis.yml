language: bash
services: docker
script:
  - ./mvnw -DskipTests package
  - docker-compose build
before_deploy:
  - echo "${DOCKER_PASSWORD}" | docker login --username "${DOCKER_USERNAME}" --password-stdin
deploy:
  provider: script
  script:
    docker push lopesgabriel/rating-spring-test;
  on:
    branch: main
notifications:
  email: false